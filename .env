# ASP.NET Core backend base URL (used once DEMO_MODE=false)
VITE_API_URL=http://localhost:5000/api

# When true, the app runs entirely on in-memory mock data — no backend needed.
# Set to false to hit your real API and SignalR hub.
VITE_DEMO_MODE=false